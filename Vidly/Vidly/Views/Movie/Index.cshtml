@model IEnumerable<Vidly.Models.Movie>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>


<ol>
    @foreach (var movie in Model)
    {
        <li>@Html.ActionLink(movie.Name, "Details", "Movie", new { id = movie.Id }, null)</li>
       
    }
</ol>
